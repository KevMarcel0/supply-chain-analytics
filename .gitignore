# Python virtual environment (machine-specific, large — never commit)
.venv/
__pycache__/
*.pyc

# OS noise
.DS_Store

# Keep the data CSVs and charts (they're part of the portfolio),
# but the SQLite db is regenerated from them, so it's optional to commit.
# Uncomment the next line if you'd rather not commit the binary db:
# supplychain.db

# Real dataset is large (~91 MB) and re-downloadable — fetch with download_data.py
realdata/

# Cleaned Power BI file (~40 MB) is regenerated by prep_powerbi_data.py
powerbi/powerbi_dataco.csv
